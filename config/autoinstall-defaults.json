{
  "$comment": "Default centralizzati per la generazione dell'ISO autoinstall (scripts/build-iso.sh). Ogni valore qui e' un default: i flag CLI di build-iso.sh, quando passati, hanno sempre precedenza su questo file. Vedi README/issue #1 e #2 per il contesto di ciascun campo.",

  "ubuntu_version": "24.04.2",

  "identity": {
    "$comment": "hostname_prefix e' usato a build-time (placeholder __HOSTNAME_PREFIX__ in iso/user-data), ma l'hostname finale <prefix>-XXXX (XXXX: fino a 4 caratteri alfanumerici casuali) viene generato a INSTALL-time via late-commands, non qui: la stessa ISO puo' installare piu' nodi fisici diversi (es. da chiavetta USB, vedi docs/usb-boot.md), un hostname fisso nell'ISO farebbe collidere piu' nodi sulla stessa rete.",
    "hostname_prefix": "berlin",
    "username": "admin"
  },

  "storage": {
    "$comment": "disk_topology seleziona quale frammento iso/storage-<topology>-disk.yaml viene iniettato in iso/user-data. 'single': un solo disco fisico (sistema + datastore sullo stesso disco). 'dual': due dischi fisici (sistema su uno, datastore sull'altro) - vedi issue #2.",
    "disk_topology": "single",
    "system_partition_size": "100G",
    "datastore": {
      "$comment": "label: XFS limita la label a 12 caratteri (mkfs.xfs -L fallisce oltre); 'datastore' (9) ci sta comodo. Un solo datastore per nodo in questa fase, nessun suffisso numerico necessario.",
      "filesystem": "xfs",
      "label": "datastore",
      "mount_root": "/grastorp/volumes",
      "symlink_name": "datastore"
    }
  }
}
